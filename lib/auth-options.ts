/**
 * Re-export authOptions from the auth subdirectory for backwards compatibility.
 * The actual implementation lives in lib/auth/auth-options.ts
 */
export { authOptions } from '@/lib/auth/auth-options'
